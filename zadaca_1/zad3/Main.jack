class Main
{
    function void main()
    {
        var Clock clock;
        let clock = Clock.new(0, 0);
        do clock.drawClock();
        while (true)
        {
            do clock.tick();
            do Sys.wait(1000); // 1 sekunda = 1 minuta
        }
        return;
    }
}